all:
    hosts:
        site1:
            root_folder: ./sites/site1 
            use_sops: True
        site2:
            root_folder: ~/src/k14/ansible/sites/site2 
    vars:
        ansible_connection: local
        ansible_python_interpreter: "{{ansible_playbook_python}}"
        site_file: site.yaml

metallb:
    hosts:
        site1:
        site2:
